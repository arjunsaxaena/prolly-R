v <- c(1, 2, 3, 4, 5) # create a vector

sum(v) # sum of the vector
min(v) # minimum of the vector
max(v) # maximum of the vector

v[i] # get the i-th element of the vector
v[-(2:4)] # remove the 2nd to 4th element